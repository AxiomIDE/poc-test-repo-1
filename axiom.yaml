name: axiom-official/axiom-text-ops
version: 0.1.0
language: go
nodes:
    - name: Tokenize
      input: TextRequest
      output: TokensResult
      required_secrets:
        - MY_SECRET
    - name: Chunk
      input: TextRequest
      output: TokensResult
      type: pipeline
